<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="ReviewMapper">

	<resultMap id="review" type="project.spring.hohotest.model.Review">
		<result property="id" column="id" />
		<result property="member_id" column="member_id" />
		<result property="product_id" column="product_id" />
		<result property="title" column="title" />
		<result property="content" column="content" />
		<result property="rating" column="rating" />
		<result property="image" column="image" />
		<result property="hit" column="hit" />
		<result property="reg_date" column="reg_date" />
		<result property="edit_date" column="edit_date" />
	</resultMap>
	
	<select id="selectReviewListByProductId" parameterType="project.spring.hohotest.model.Review"
		resultMap="review">
		SELECT	id, image, title, rating, edit_date, hit, member_id from review where product_id=#{product_id}
		ORDER BY id desc
	</select>
</mapper>